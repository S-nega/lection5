extends layout

block content

  if currentuser
    p Hello, #{currentuser.name} !
    <a href="/users/auth">Log Out</a>
    br
    br
    a(href="/files/write/file") add new file
    br
    a(href="/files/write/fold") add new folder
  else 
    p You are not registered
    <a href="/users/auth">Log In to your account</a>
    br
    <a href="/users/reg">Don't have an account?</a>
    
    br
  br

  br
  a(href="/users") back to users

  br
  br

  if dir 
    a(href="/files/read") back to list
    br
    br

    p #{dir}
    if currentuser
      form(action="/files/del/#{dir}", method="post")
          input(type="submit" value="del") 

      form(action="/files/edit/#{dir}", method="get")
        input(type="submit" value="edit")


  p #{d}
  each d in data

    //- form(action="/files/read/#{d}" method="get")
    //-   input(type="text" visibility="hidden" name="dir" value="#{dir}") 
    //-   input(type="hidden" name="name" value="#{d}") 
    //-   input(type="submit" value="#{d}")
    p 
      //- form(action="/files/read/#{dir}/#{d}", method="get")
      //-   //- if dir 
      //-   input(type="hidden" name="direct" value="#{dir}")
      //-   //- else 
      //-     input(type="hidden" name="dir" value="null")
      //-   input(type="submit" value="#{d}")
      //- <a href="/files/read/#{d}">#{d} 

    if dir 
      a(href="/files/read/#{dir}/#{d}") #{d}
    else 
      a(href="/files/read/#{d}") #{d} 
      